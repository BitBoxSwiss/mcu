language: c
sudo: false
os:
    - osx
    - linux
    
compiler:
    - clang
    - gcc

matrix:
    exclude:
        - os: osx
          compiler: gcc
    fast_finish:
        - true

script: 
    - mkdir build && cd build
    - cmake .. -DCONTINUOUS_INTEGRATION=1 && make
    - make test
