language: c
sudo: false
os:
    - osx
    - linux
    
compiler:
    - clang
    - gcc

matrix:
    exclude:
        - os: osx
          compiler: gcc
    fast_finish:
        - true

env: TRAVIS_BUILD=1

script: 
    - make
    - ./tests_unit 
    - ./tests_openssl 200
    - ./tests_api
